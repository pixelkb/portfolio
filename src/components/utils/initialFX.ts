export const initialFX = () => {
  // This function is called immediately after the loading screen completes
  // to trigger page entry animations.
  console.log("Page entrance animations initialized.");
};
